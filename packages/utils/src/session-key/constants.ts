/** One year in milliseconds. */
export const SESSION_KEY_EXPIRY_MS = 365 * 24 * 60 * 60 * 1000;

/** Current transfer payload schema version. */
export const SESSION_KEY_TRANSFER_PAYLOAD_VERSION = 1;

/** Default transfer payload validity window: two minutes. */
export const SESSION_KEY_TRANSFER_MAX_AGE_MS = 120_000;
