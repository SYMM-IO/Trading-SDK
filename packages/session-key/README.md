# @symmio/session-key

Framework-agnostic local EVM session-key helpers for SYMMIO SDK consumers.

This package owns key generation, import, runtime manager state, raw message signing, EIP-712 signing, and transfer payload helpers. Applications own persistence and encryption.
