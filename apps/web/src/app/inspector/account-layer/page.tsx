import { InspectorShell } from "@/features/inspector/inspector-shell";

export const metadata = {
  title: "AccountLayer · Symmio Inspector",
  description: "Live integration page for the SYMMIO React SDK's AccountLayer slice.",
};

export default function AccountLayerInspectorPage() {
  return <InspectorShell />;
}
