# Muon oracle

Muon is a decentralized oracle network. SYMMIO uses it to attest to prices, unrealized-PnL (uPnL) values, and account overviews that the contract will only accept when signed by a trusted quorum (`removeMargin` / `deallocate`, settlement, force-close, liquidation).

`@symmio/trading-core` exposes each documented Muon endpoint as its own typed read. Every method resolves the oracle gateway URLs and the SYMMIO diamond address (the `symmio` request param) from `config`, tries the configured gateways in order until one succeeds, and returns a **normalized attestation** — typed `bigint` / `Address` fields plus the shared signature envelope. The raw envelope fields common to every return are documented once on [`MuonAttestationBase`](/core/muon/muon-attestation-base).

Each method has its own page with the full signature, parameters, return shape, examples, and query options.

## Prices

<MethodIndex kind="read">
  <MethodEntry name="getMuonPrice" href="/core/muon/price">
    Validated prices for a set of quote ids.
  </MethodEntry>
  <MethodEntry name="getMuonPriceRange" href="/core/muon/price-range">
    TWAP-style price range over a time window for force-close validation.
  </MethodEntry>
</MethodIndex>

## uPnL attestations

<MethodIndex kind="read">
  <MethodEntry name="getMuonUpnl" href="/core/muon/upnl">
    Combined partyA + partyB unrealized PnL in one attestation.
  </MethodEntry>
  <MethodEntry name="getMuonUpnlA" href="/core/muon/upnl-a">
    A partyA's unrealized-PnL attestation.
  </MethodEntry>
  <MethodEntry name="getMuonUpnlB" href="/core/muon/upnl-b">
    A partyB's unrealized PnL computed against a partyA.
  </MethodEntry>
  <MethodEntry name="getMuonUpnlWithSymbolPrice" href="/core/muon/upnl-with-symbol-price">
    Both parties' uPnL plus a single symbol price.
  </MethodEntry>
  <MethodEntry name="getMuonUpnlAWithSymbolPrice" href="/core/muon/upnl-a-with-symbol-price">
    A partyA's uPnL plus a single symbol price.
  </MethodEntry>
  <MethodEntry name="getMuonSettleUpnl" href="/core/muon/settle-upnl">
    A partyA's uPnL settlement data for a set of quotes.
  </MethodEntry>
  <MethodEntry name="getMuonPartyAOverview" href="/core/muon/party-a-overview">
    A partyA's liquidation overview.
  </MethodEntry>
</MethodIndex>

## Contract signatures

<MethodIndex kind="read">
  <MethodEntry name="getDeallocateUpnlSig" href="/core/muon/deallocate-upnl-sig">
    A `uPnl_A` attestation assembled into the contract-ready `SingleUpnlSig` that `removeMargin` / `deallocate` need.
  </MethodEntry>
  <MethodEntry name="getSendQuoteUpnlSig" href="/core/muon/send-quote-upnl-sig">
    A `uPnl_A_withSymbolPrice` attestation assembled into the `SingleUpnlAndPriceSig` that `sendQuote` needs.
  </MethodEntry>
  <MethodEntry name="getForceClosePriceSig" href="/core/muon/force-close-price-sig">
    A `priceRange` attestation assembled into the `HighLowPriceSig` that `forceClosePosition` verifies.
  </MethodEntry>
</MethodIndex>

## Types

<MethodIndex kind="type">
  <MethodEntry name="MuonAttestationBase" href="/core/muon/muon-attestation-base">
    The signature envelope every normalized attestation return extends.
  </MethodEntry>
</MethodIndex>

## Query options

Each read ships a matching `…QueryOptions` factory and a `…QueryKey` builder for TanStack Query. See each method's page for the exact call, and [Query options](/core/concepts/query-options) for the shared pattern. Attestations are timestamped and short-lived — cache them with a low `staleTime`, and for a signature you intend to submit on-chain fetch a fresh one on demand rather than serving a cached one.

## Errors

- `MUON_URLS_NOT_CONFIGURED` — the chain has no Muon gateway URLs in its config. Surfaces as a `SymmError` with `kind: "config"`.
- `MUON_REQUEST_UNSUCCESSFUL` — a gateway responded but the attestation was unsuccessful (quorum failure, invalid request). Surfaces as a `SymmError` with `kind: "api"`.
- `FETCH_MUON_REQUEST_FAILED` — every configured gateway failed at the transport / HTTP layer. Surfaces as a `SymmApiError` (`kind: "api"`) carrying the last transport error.

## Related

- [SYMMIO Contract](/core/symmio-contract) — writes that consume Muon attestations (`removeMargin`, settle, force-close).
- [AccountLayer](/core/account-layer) — `removeMargin` / `deallocate`, which need `getDeallocateUpnlSig`.
- [Errors](/core/concepts/errors) — the full `SymmError` catalogue.
