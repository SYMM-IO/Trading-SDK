import { GaslessScreen } from "@/features/gasless/gasless-screen";

/** Gasless onboarding, fee controls and relay observability. */
export default function GaslessPage() {
  return <GaslessScreen />;
}
