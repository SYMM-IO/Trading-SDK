import { ActivityScreen } from "@/features/activity/activity-screen";

/** Merged history and live event stream across every SYMMIO deployment Prism reads. */
export default function Page() {
  return <ActivityScreen />;
}
